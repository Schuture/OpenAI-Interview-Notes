# 参与贡献

[English](CONTRIBUTING.md) · [中文](CONTRIBUTING.zh.md)

感谢你的帮助。参与的方式有三种，投入从少到多。

1. **报告错误。** [提一个 issue](https://github.com/Schuture/OpenAI-Interview-Notes/issues)，附上页面链接、有问题的句子或代码行、错在哪里；能给出正确写法就更好。
2. **补充变体或新题。** 提一个 issue，用你自己的话描述：任务是什么、分几个部分、有哪些约束、问了哪些追问。
   不要写任何能定位到面试官或候选人的信息。
3. **提交 pull request**，修改或新增页面，遵守下面的规则。

## 内容规则

1. **事实可以写，别人的措辞不能搬。** 一轮面试考什么、接口长什么样、约束、已知变体、时间限制，这些是事实，可以写。
   来自备考网站、付费资料或他人帖子的文字、结构、示例数据和代码不能搬。全部用自己的话重述，示例自己编。
2. **不放面试材料。** 不要粘贴面试时拿到的 starter 文件或测试 harness，只描述接口。
3. **写了就要跑。** 解法从零写，并且必须通过它的检查代码（见下文）。
4. **不涉及个人信息。** 不写人名、不写精确日期，不写任何能定位到候选人或面试官的信息。
5. **中英文都要有。** 英文页是基准，中文页的标题与它一一对应。两个文件里的每个 `python` 和 `py` 代码块完全一致，
   注释用英文。如果你只能写其中一种语言，照样提交 pull request，并在说明里写明。

## 一页怎么写

每一页只有两节。

**题目。** 完整的正式题面，按教材或竞赛题的方式来陈述。

1. 从题目里的对象写起：输入、它的形状、要计算的量。不要用关于这一轮面试或页面本身的话开头。
2. 使用教材和相关库官方文档里的标准术语。术语在第一次用到的地方用一句话定义。中文页里术语首次出现时在括号里给出标准英文。
3. 每个部分一个块：规则或任务、函数签名、一个例子。规则涉及时间或先后顺序时，要有一个逐步推演的例子。
4. 不写旁白：解释措辞的句子、说明题目出处的句子、谈论页面本身的句子都不要写。题型写在 `meta.yaml` 的 `kind` 里，
   由页面顶部的表格显示。
5. 陈述任务的文字不要省；其余的文字能省则省。

**参考解答。** 与答案有关的内容全部放在这一节，整节包在默认折叠的 `<details>` 里，按十五分钟左右能读完来写。

1. 开头写出值得向面试官确认的一两个点。
2. 然后按部分写：思路、推导（有的话要推导，不要直接断言）、代码。
3. 技术性的易错点写成代码里的 `# NOTE:` 注释，放在相关的那一行，不单独列清单。
4. 最后用几条一行的追问收尾。不要总结，不要清单。
5. 检查代码放进末尾嵌套的第二个 `<details>` 块：针对示例的断言；能写暴力解的，再加一个只依据题面写出的暴力解
   （不能调用解法里的辅助函数）做对拍。系统设计题要把正文引用的每个估算都用代码重算一遍。

代码围栏：标记为 `python` 的代码块会被执行，必须能跑通；标记为 `py` 的代码块只作示意（只有签名的函数、故意报错的代码）。
一页里所有 `python` 代码块在同一个命名空间里按顺序运行，在笔记本 CPU 上合计应远低于 30 秒。

## 新增一道题

一道题是一个目录 `<category>/<slug>/`，里面有三个文件：`meta.yaml`、`README.md` 和 `README.zh.md`。
最省事的做法是复制一页同类的题目再改。语言切换行下面的 `<!-- meta:begin -->` 和 `<!-- meta:end -->` 两行要保留，
两行之间的表格是自动生成的。

| `meta.yaml` 字段 | 含义 |
| --- | --- |
| `title`、`title_zh` | 页面标题；两个 README 的第一行必须是 `# <标题>` |
| `summary`、`summary_zh` | 一句话说明这道题要做什么，显示在学习路线里 |
| `category` | `coding`、`system-design` 或 `behavioral`，必须与所在目录一致 |
| `kind`、`kind_zh` | 页头显示的题型，例如 `编程（含数学推导）· NumPy` |
| `roles` | 从 `RS`、`RE`、`MLE`、`SWE`、`Infra Eng`、`EM`、`All` 中选 |
| `topics` | 简短的标签，例如 `heap`、`idempotency` |
| `difficulty` | `easy`、`medium` 或 `hard`（可选） |
| `frequency` | `very-high`、`high`、`medium`、`low` 或 `rare`，显示为 ★★★★★ 到 ★☆☆☆☆ |
| `format` | 部分数，以及已知的整轮时长，例如 `4 parts / 60 min`（可选） |
| `stage` | 从 `phone-screen`、`tech-screen`、`recruiter-screen`、`onsite` 中选（可选） |
| `order` | 在同一频率档内的排序位置 |

要把这道题放进学习路线，编辑 `scripts/roadmap.yaml`。

## 提交 pull request 之前

```bash
pip install -r requirements.txt
python scripts/build_index.py                              # 重新生成页头表格、README 题目表和学习路线
python scripts/check_i18n.py                               # 中英文是否一致，链接和锚点是否有效
python scripts/check_render.py                             # 强调符号和折叠块在 GitHub 上能否正常渲染
python scripts/run_snippets.py coding/<slug>               # 运行这一页的代码
python scripts/run_snippets.py --hash-seeds coding/<slug>  # 有随机性或用到集合的页面
```

`--hash-seeds` 会在两个不同的字符串哈希种子下各跑一遍：结果不同，说明代码某处依赖了集合的遍历顺序。
每个 pull request 都会由 CI 跑一遍上面这些检查。
